#include<bits/stdc++.h>
using namespace std;
int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    long int T;
    cin>>T;
    while(T--)
    {
        long long int n,a=0,b=0;
        cin>>n;
        cout<<a<<" "<<b<<"\n";
    }

    return 0;
}